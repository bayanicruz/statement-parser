[project]
name = "statement-parser"
version = "0.1.0"
description = "Extract transactions from bank credit card statement PDFs into CSV"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.12"
authors = [{ name = "Bayani Cruz" }]
keywords = ["bank", "bank", "statement", "pdf", "csv"]
dependencies = [
    "odfpy>=1.4.1",
    "openpyxl>=3.1.5",
    "pandas>=3.0.3",
    "pdfplumber>=0.11.9",
]

[project.scripts]
statement-parser = "statements:main"
transaction-tracker = "transaction:main"
transaction-tracker-pdf = "transaction_v2:main"
